package Test2;

import javafx.scene.shape.Rectangle;

import java.awt.*;

public class rec extends Rectangle {
    public rec(int H, int W, int X, int Y){
        setHeight(H);
        setWidth(W);
        setTranslateX(X);
        setTranslateY(Y);
    }
}
